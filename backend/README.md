```zsh
1. brew install go
2. Notion에서 `euisu → launchctl로 자동 실행하기` 문서 찾아서 launchctl 파일 추가
3. brew install --cask google-cloud-sdk
4. gcloud auth application-default login
5. gcloud auth application-default set-quota-project
```
